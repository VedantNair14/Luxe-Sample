{
  "name": "luxe-clothing-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -F frontend dev",
    "build": "pnpm -F frontend build",
    "start": "pnpm -F frontend start",
    "lint": "pnpm -F frontend lint",
    "backend-dev": "cd backend && python main.py"
  },
  "dependencies": {
  },
  "engines": {
    "node": "20.x"
  }
}
